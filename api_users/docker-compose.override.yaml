version: "3.5"

services:
    backend:
        restart: "no"
        ports:
            - "8888:8888"
        volumes:
            - ./:/opt/backend
        build:
            context: .
            args:
                INSTALL_DEV: ${INSTALL_DEV-true}
